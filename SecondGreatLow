def SecondGreatLow(arr)

  return arr.sort.reverse[1,2].join(' ')
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
SecondGreatLow(STDIN.gets)           
